{
  "compilerOptions": {
    // Use ${configDir} to make paths relative to the tsconfig.json location
    "outDir": "${configDir}/dist",
    "rootDir": "${configDir}/src"
  },
  "include": ["${configDir}/src/**/*"],
  "exclude": ["${configDir}/node_modules", "${configDir}/dist"]
}
